import React from "react";
import { TouchableOpacity, Text } from "react-native";

function ButtonSave() {
  return (
    <TouchableOpacity>
      <Text>Save</Text>
    </TouchableOpacity>
  );
}

export default ButtonSave;
